import React from 'react';

const Features = ({ data }) => {
    return (
        <div>
            <p className='text-xl font-sans font-bold text-white/80'>{data}</p>
        </div>
    );
};

export default Features;